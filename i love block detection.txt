 ARRAY OF OBJECTS

object:
x
y
width 
height
type

all break conditions

playerX < objects[i].x - 100

playerX > objects[i].x + objects[i].width + 100

playerY < objects[i].y - 100

playerY > objects[i].y + objects[i].height + 100


hitDetection = false;

playerX >= blockX && playerX <= blockX+width && playerY >= blockY && playerY <= blockY+height
	hitDetection = true;
	break;
playerX+width >= blockX && playerX+width <= blockX+width && playerY >= blockY && playerY <= blockY+height
	hitDetection = true;
	break;
playerX >= blockX && playerX <= blockX+width && playerY+height >= blockY && playerY+height <= blockY+height
	hitDetection = true;
	break;
playerX+width >= blockX && playerX+width <= blockX+width && playerY+height >= blockY && playerY+height <= blockY+height
	hitDetection = true;
	break;

if(hitDetection){
	if(object[i].type == portal){
		player.jumpPortal();
	}
	if(object[i].type == block){
		block.move();
	}
}